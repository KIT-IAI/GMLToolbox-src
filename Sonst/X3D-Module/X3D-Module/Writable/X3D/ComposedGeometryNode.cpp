#pragma once
#include "ComposedGeometryNode.h"

namespace X3D
{
	/*
	IDable* ComposedGeometryNode::getElementById(const std::string ident)
	{
		std::string ownId = getID();

		if (ident == ownId)
		{
			return this;
		}
		else
		{
			return m_coordinate.getElementById(ident);
		}
	}
	*/
}